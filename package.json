{
  "name": "haraka.github.com",
  "license": "MIT",
  "version": "1.0.0",
  "scripts": {
    "dev": "nuxt",
    "build": "nuxt build",
    "start": "nuxt start",
    "generate": "nuxt generate",
    "deploy": "push-dir --dir=dist --branch=master --cleanup"
  },
  "dependencies": {
    "@nuxt/content-theme-docs": "^0.5.1",
    "nuxt": "^2.14.3"
  },
  "devDependencies": {
    "push-dir": "^0.4.1"
  }
}
